"""Router-KD plugin implementations.

Empty at the RK-1 scaffold stage. The unified Router-KD algorithm — the
KD training loop serving both Stage 2.5 and Stage 5 — is extracted from the
legacy ``stage5_router_kd.py`` monolith into focused plugins here by tasks
RK-2..RK-7.
"""
