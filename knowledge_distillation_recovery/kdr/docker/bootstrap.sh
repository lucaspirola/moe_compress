#!/usr/bin/env bash
# kdr bootstrap.sh — Phase 6 placeholder.
#
# Implementation lands in Phase 6 per the plan. This stub exists in Phase 2 only
# to make the directory tree visible for review. See LLR-0008 (run_id +
# resume), LLR-0032 (env-var validation), LLR-0033 (HF Hub partials query),
# LLR-0035 (Zyphra transformers fork install).
set -euo pipefail
echo "kdr bootstrap.sh: Phase 6 stub. See LLR-0032..LLR-0035." >&2
exit 64  # EX_USAGE — clearly indicates this is a not-yet-wired stub.
