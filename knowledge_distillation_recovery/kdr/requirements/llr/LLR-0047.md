---
id: LLR-0047
type: llr
title: "pre-commit hook running mypy and ruff"
status: draft
parent: HLR-0012
---

## Description

`.pre-commit-config.yaml` also runs `mypy --strict` and `ruff check` on every commit.

## Rationale

Static typing and linting enforced at commit time, not just CI.

## Acceptance Criteria

- [ ] Hook fails the commit on any mypy or ruff violation
- [ ] Hook respects the pyproject.toml configuration
