---
id: LLR-0020
type: llr
title: "quantization_config injection into config.json"
status: draft
parent: HLR-0005
---

## Description

`config.json` of the output gets a `quantization_config` block populated with `quant_method='compressed-tensors'` and per-quantizer schema mirroring the YAML's quant block.

## Rationale

Standardised HF-loadable config; no kdr-internal magic.

## Acceptance Criteria

- [ ] `config_groups.weights` reflects WeightQuantSpec
- [ ] `kv_cache_scheme` reflects the K/V specs
- [ ] An `ignore` list reflects the adapter's FP32 carve-outs
