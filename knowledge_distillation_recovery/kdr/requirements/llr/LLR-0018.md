---
id: LLR-0018
type: llr
title: "compressed-tensors safetensors writer"
status: draft
parent: HLR-0005
---

## Description

`kdr.io.save.save_kdr_artifact(model, output_dir, recipe, input_metadata)` writes sharded compressed-tensors safetensors plus `config.json` with `quantization_config` and the carried-forward `compressed_metadata.json`.

## Rationale

Single emit point keeps output format consistent.

## Acceptance Criteria

- [ ] Output dir validates as a `compressed-tensors` HF checkpoint
- [ ] Round-trip via `AutoModelForCausalLM.from_pretrained` succeeds on the test fixture
