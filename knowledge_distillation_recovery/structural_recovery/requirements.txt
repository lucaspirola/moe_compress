# Local-dev pin reference. Production installs use pyproject.toml.
# torch / transformers / accelerate / datasets / safetensors / hf_hub already
# present in /home/lucas/ai/venv (shared with max_quality). Listed for record.
#
# Chapter 1-specific extras:
bitsandbytes>=0.44.0          # 8-bit AdamW (Appendix D.4)
deepspeed>=0.15.0             # ZeRO-3 across 4x A100
nvidia-modelopt>=0.21.0       # LogitsDistillationLoss (guide §1.8.2)
pyyaml>=6.0
pytest>=8.0
